node_modules/
.next/
out/
.env
.env.local
.DS_Store
*.log
public/sitemap*.xml
public/robots.txt
token.json 
client_secret*.json 
.vercel
.env*

# Local Claude Code tool state (permission cache, dev-server launch config).
# Spans multiple unrelated projects on this machine and has previously held a
# plaintext secret in its permission-cache history — must stay machine-local.
.claude/
